[
  {
    "tool_id": "memory_search",
    "name": "Memory Search",
    "description": "Vektör belleğinden cosine similarity ile en yakın dokümanları getirir (kullanıcı mesajları, geçmiş agent çıktıları, manuel notlar).",
    "category": "memory",
    "provider": "internal",
    "input_schema": {
      "type": "object",
      "properties": {
        "query": {"type": "string", "description": "Arama metni."},
        "k": {"type": "integer", "minimum": 1, "maximum": 25, "default": 5},
        "kind": {
          "type": "string",
          "enum": ["user_message", "agent_output", "tool_result", "doc"],
          "description": "Sadece bu türdeki dokümanları döndür."
        }
      },
      "required": ["query"]
    },
    "output_schema": {
      "type": "object",
      "properties": {
        "matches": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "id": {"type": "string"},
              "kind": {"type": "string"},
              "agent_id": {"type": ["string", "null"]},
              "task_id": {"type": ["string", "null"]},
              "text": {"type": "string"},
              "score": {"type": "number"}
            }
          }
        }
      }
    },
    "allowed_agents": [],
    "cost_estimate_usd": 0.0001,
    "mode": "live",
    "tags": ["memory", "rag"]
  },
  {
    "tool_id": "knowledge_search",
    "name": "Knowledge Search",
    "description": "Ürüne ait bilgi tabanında (marka kılavuzları, ürün özellikleri, iş kuralları, geçmiş kararlar) semantik arama yapar.",
    "category": "memory",
    "provider": "internal",
    "input_schema": {
      "type": "object",
      "properties": {
        "query": {"type": "string", "description": "Arama sorusu veya anahtar terim."},
        "k": {"type": "integer", "minimum": 1, "maximum": 20, "default": 5},
        "product_id": {
          "type": "string",
          "description": "Aramayı belirli bir ürünle sınırla (opsiyonel)."
        }
      },
      "required": ["query"]
    },
    "output_schema": {
      "type": "object",
      "properties": {
        "results": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "id": {"type": "string"},
              "text": {"type": "string"},
              "score": {"type": "number"},
              "product_id": {"type": ["string", "null"]}
            }
          }
        },
        "total": {"type": "integer"}
      }
    },
    "allowed_agents": [],
    "cost_estimate_usd": 0.0001,
    "mode": "live",
    "tags": ["memory", "rag", "knowledge"]
  }
]
