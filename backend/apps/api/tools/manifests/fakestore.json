[
  {
    "tool_id": "fakestore_list_products",
    "name": "FakeStore Ürünleri Listele",
    "description": "fakestoreapi.com'dan örnek ürünleri listeler. Kategori, limit ve sıralama destekler. Gerçek mağaza olmadan rakip/katalog testleri için.",
    "category": "store",
    "provider": "fakestore",
    "auth_required": false,
    "timeout_ms": 15000,
    "input_schema": {
      "type": "object",
      "properties": {
        "limit": {"type": "integer", "minimum": 1, "maximum": 50, "default": 10},
        "sort": {"type": "string", "enum": ["asc", "desc"], "description": "id'ye göre sıralama yönü"},
        "category": {"type": "string", "description": "Belirli bir kategoriyle filtreler (örn. electronics)"}
      },
      "required": []
    },
    "output_schema": {
      "type": "object",
      "properties": {
        "count": {"type": "integer"},
        "products": {"type": "array"}
      }
    },
    "allowed_agents": ["catalog_agent", "market_research_agent", "pricing_finance_agent", "supervisor"],
    "cost_estimate_usd": 0.0,
    "mode": "live",
    "tags": ["store", "fakestore", "sample", "catalog"]
  },
  {
    "tool_id": "fakestore_get_product",
    "name": "FakeStore Ürün Detayı",
    "description": "Tekil bir ürünün detayını getirir (fiyat, açıklama, görsel, kategori, rating).",
    "category": "store",
    "provider": "fakestore",
    "auth_required": false,
    "timeout_ms": 10000,
    "input_schema": {
      "type": "object",
      "properties": {
        "id": {"type": "integer", "minimum": 1, "description": "Ürün ID'si"}
      },
      "required": ["id"]
    },
    "output_schema": {
      "type": "object",
      "properties": {
        "id": {"type": "integer"},
        "title": {"type": "string"},
        "price": {"type": "number"},
        "category": {"type": "string"},
        "description": {"type": "string"},
        "image": {"type": "string"},
        "rating": {"type": "object"}
      }
    },
    "allowed_agents": ["catalog_agent", "market_research_agent", "pricing_finance_agent", "supervisor"],
    "cost_estimate_usd": 0.0,
    "mode": "live",
    "tags": ["store", "fakestore", "sample", "product"]
  },
  {
    "tool_id": "fakestore_list_categories",
    "name": "FakeStore Kategorileri",
    "description": "Mevcut ürün kategorilerini döner (örn. electronics, jewelery, men's clothing, women's clothing).",
    "category": "store",
    "provider": "fakestore",
    "auth_required": false,
    "timeout_ms": 10000,
    "input_schema": {
      "type": "object",
      "properties": {},
      "required": []
    },
    "output_schema": {
      "type": "object",
      "properties": {
        "count": {"type": "integer"},
        "categories": {"type": "array"}
      }
    },
    "allowed_agents": ["catalog_agent", "market_research_agent", "supervisor"],
    "cost_estimate_usd": 0.0,
    "mode": "live",
    "tags": ["store", "fakestore", "sample", "categories"]
  },
  {
    "tool_id": "fakestore_list_carts",
    "name": "FakeStore Sepetleri",
    "description": "Örnek sepet verilerini döner (user_id, ürünler, miktar). Sipariş/dönüşüm analizleri için test verisi.",
    "category": "store",
    "provider": "fakestore",
    "auth_required": false,
    "timeout_ms": 15000,
    "input_schema": {
      "type": "object",
      "properties": {
        "limit": {"type": "integer", "minimum": 1, "maximum": 50, "default": 5},
        "sort": {"type": "string", "enum": ["asc", "desc"]}
      },
      "required": []
    },
    "output_schema": {
      "type": "object",
      "properties": {
        "count": {"type": "integer"},
        "carts": {"type": "array"}
      }
    },
    "allowed_agents": ["operations_agent", "growth_review_agent", "market_research_agent", "supervisor"],
    "cost_estimate_usd": 0.0,
    "mode": "live",
    "tags": ["store", "fakestore", "sample", "carts", "orders"]
  },
  {
    "tool_id": "fakestore_list_users",
    "name": "FakeStore Kullanıcıları",
    "description": "Örnek kullanıcı verilerini döner (email, ad, telefon — şifre alanı çıkarılır). Persona/segment denemeleri için.",
    "category": "store",
    "provider": "fakestore",
    "auth_required": false,
    "timeout_ms": 15000,
    "input_schema": {
      "type": "object",
      "properties": {
        "limit": {"type": "integer", "minimum": 1, "maximum": 20, "default": 5}
      },
      "required": []
    },
    "output_schema": {
      "type": "object",
      "properties": {
        "count": {"type": "integer"},
        "users": {"type": "array"}
      }
    },
    "allowed_agents": ["market_research_agent", "growth_review_agent", "supervisor"],
    "cost_estimate_usd": 0.0,
    "mode": "live",
    "tags": ["store", "fakestore", "sample", "users", "personas"]
  }
]
