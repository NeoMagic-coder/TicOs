import { AppShell } from "@/components/layout/app-shell";
import { VisualStudioView } from "@/components/visual-studio/visual-studio-view";

export default function VisualStudioPage() {
  return (
    <AppShell>
      <VisualStudioView />
    </AppShell>
  );
}
